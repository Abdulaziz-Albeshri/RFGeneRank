% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crossval.R
\name{rfgr_crossval}
\alias{rfgr_crossval}
\title{Batch-aware cross-validation with optional ComBat modes (incl. frozen ComBat)}
\usage{
rfgr_crossval(
  expr,
  metadata,
  label_col = "state",
  batch_col = "batch",
  covariates = NULL,
  cv = c("lobo", "groupk", "kfold"),
  k = 5,
  combat_mode = c("none", "train"),
  rf_trees = 1000,
  seed = 1,
  verbose = TRUE
)
}
\arguments{
\item{expr}{numeric matrix genes x samples (continuous scale: log2CPM or log2(TPM+1))}

\item{metadata}{data.frame with SampleID, label_col, batch_col}

\item{label_col}{character; target column in metadata (e.g., "state")}

\item{batch_col}{character; batch/dataset column in metadata (e.g., "batch")}

\item{covariates}{character vector of column names to \emph{preserve} in ComBat (added to design)}

\item{cv}{one of c("lobo","groupk","kfold")}

\item{k}{integer; number of folds for "groupk" or "kfold"}

\item{combat_mode}{one of c("none","train")}

\item{rf_trees}{integer; number of trees for ranger}

\item{seed}{integer; RNG seed}

\item{verbose}{logical; emit progress messages}
}
\value{
list(auc_by_fold, mean_auc, settings, folds_info)
}
\description{
Orchestrates leakage-safe CV for RFGeneRank with two ComBat modes:
\itemize{
\item "none":     no batch correction
\item "train":    frozen ComBat - fit on TRAIN folds only; apply TRAIN parameters to TEST
}

Supports LOBO (leave-one-batch-out), Group K-Fold by batch, and standard K-Fold.
}
\examples{

# Minimal runnable example: tiny dataset, fast evaluation
expr <- matrix(rnorm(10 * 6), nrow = 10)
rownames(expr) <- paste0("gene", 1:10)
colnames(expr) <- paste0("sample", 1:6)

label <- factor(rep(c("A", "B"), each = 3))
batch <- factor(rep(c("batch1", "batch2"), times = 3))

metadata <- data.frame(
  label = label,
  batch = batch,
  row.names = colnames(expr)
)

# Lightweight cross-validation: no ComBat, few trees
cv_res <- rfgr_crossval(
  expr        = expr,
  metadata    = metadata,
  label_col   = "label",
  batch_col   = "batch",
  cv          = "kfold",
  k           = 2,
  combat_mode = "none",
  rf_trees    = 10,
  verbose     = FALSE
)

cv_res
}
