% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sign_importance.R
\name{sign_importance}
\alias{sign_importance}
\title{Signed feature importance for RFGeneRank models}
\usage{
sign_importance(
  fit,
  X,
  y = NULL,
  method = c("mean", "de", "shap"),
  de_table = NULL,
  case_level = NULL,
  orientation = c("samples_by_genes", "genes_by_samples"),
  assay_name = NULL,
  shap_n = 50,
  seed = 1
)
}
\arguments{
\item{fit}{Trained model object from rank_genes() or similar.}

\item{X}{Expression (matrix/data.frame/SummarizedExperiment).}

\item{y}{Factor labels (optional if retrievable from fit).}

\item{method}{c("mean","de","shap"). Default "mean".}

\item{de_table}{data.frame with columns gene, log2FC (for method="de").}

\item{case_level}{Positive class label (default = last level of y).}

\item{orientation}{"samples_by_genes" or "genes_by_samples".}

\item{assay_name}{SE assay name/index.}

\item{shap_n}{Integer; number of Monte Carlo samples for SHAP.}

\item{seed}{RNG seed.}
}
\value{
data.frame with gene, importance, direction, signed_importance,
mean_case, mean_ctrl, mean_diff, log2FC, shap_dir.
}
\description{
Add direction (+/-) to RF importance using group means ("mean"),
external DE log2FC ("de"), or SHAP ("shap").
}
\examples{
# Toy expression matrix: samples x genes
X <- matrix(
  rnorm(4 * 5),
  nrow = 4,
  dimnames = list(
    paste0("sample", 1:4),
    paste0("gene",   1:5)
  )
)

# Binary labels
y <- factor(c("Control", "Control", "Case", "Case"))

# Minimal "fit" object: a list with a numeric importance vector
fit <- list(
  importance = setNames(
    c(0.8, 0.6, 0.4, 0.2, 0.1),
    paste0("gene", 1:5)
  )
)

res <- sign_importance(
  fit = fit,
  X   = X,
  y   = y,
  method = "mean"
)

head(res)
# In practice, sign_importance() is called on a GeneRankFit object
# produced by the RFGeneRank workflow, for example:
#
#   fit <- gene_rank(se, genes = rownames(se), ...)
#   sig_imp <- sign_importance(fit, X = expr_matrix, y = outcome)
#   head(sig_imp)
}
