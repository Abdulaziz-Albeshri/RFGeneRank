% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots_all.R
\name{plot_sign_importance}
\alias{plot_sign_importance}
\title{Signed feature importance (directional effect)}
\usage{
plot_sign_importance(
  fit = NULL,
  tab = NULL,
  top = 30,
  map_to_symbol = FALSE,
  from = "ENTREZID",
  to = "SYMBOL",
  show_legend = TRUE,
  palette = NULL
)
}
\arguments{
\item{fit}{GeneRankFit (optional if \code{tab} is supplied)}

\item{tab}{data.frame from \code{sign_importance()} with columns:
gene, importance, direction (-1/0/1), signed_importance}

\item{top}{Integer; number of genes to show (default 30)}

\item{map_to_symbol}{Logical; map x-axis labels to SYMBOLs (default FALSE)}

\item{from}{Keytype for input IDs (default "ENTREZID")}

\item{to}{Keytype for output labels (default "SYMBOL")}

\item{show_legend}{Logical; show legend (default TRUE)}

\item{palette}{Optional named vector for fill colors, e.g.
\code{c(`-1`="#3182bd", `1`="#de2d26", `0`="#9e9e9e")}}
}
\value{
A ggplot object.
}
\description{
Visualizes direction-aware importances produced by \code{sign_importance()}.
If \code{tab} is NULL, the function will try to read \code{fit@imp} and
require a \code{signed_importance} column to be present there.
}
\examples{
# Toy signed importance table for 5 genes
signed_imp <- data.frame(
  gene        = paste0("gene", 1:5),
  importance  = c(0.5, 0.4, 0.3, 0.2, 0.1),
  signed_effect = c(0.5, -0.4, 0.3, -0.2, 0.1)
)

head(signed_imp)

\donttest{
# In practice, plot_sign_importance() is used on a GeneRankFit object
# after computing signed importances, for example:
#
#   fit <- gene_rank(se, genes = rownames(se), ...)
#   sig_imp <- sign_importance(fit, X = expr_matrix, y = outcome)
#   plot_sign_importance(fit, top_n = 20)
#
# where the sign and magnitude of each gene's contribution are visualized.
}
}
