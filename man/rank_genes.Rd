% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank_genes.R
\name{rank_genes}
\alias{rank_genes}
\title{Rank genes with batch-aware cross-validation (UMAP-free)}
\usage{
rank_genes(
  se,
  label_col = "state",
  n_top = 500,
  k = 5,
  trees = 1000,
  importance = c("permutation", "impurity"),
  class_weights = NULL,
  threads = max(1, parallel::detectCores() - 1),
  seed = 1,
  fold_batch_correction = FALSE,
  batch_col = NULL,
  batch_covariates = NULL,
  filter_low_expr = FALSE,
  min_prop = 0.2,
  transform = c("none", "log1p"),
  standardize = FALSE,
  cv = c("kfold", "lobo", "groupk"),
  auto_confounds = TRUE
)
}
\arguments{
\item{se}{SummarizedExperiment}

\item{label_col}{character; class-label column in colData(se)}

\item{n_top}{integer; per-fold top-variance feature count (0 = off)}

\item{k}{integer; number of folds (ignored by LOBO)}

\item{trees}{integer; number of trees for ranger}

\item{importance}{"permutation" or "impurity"}

\item{class_weights}{named numeric vector or NULL (auto-computed if NULL and imbalance >= 1.5x)}

\item{threads}{integer; CPU threads for ranger}

\item{seed}{integer; RNG seed}

\item{fold_batch_correction}{logical; if TRUE, do train-only batch removal per fold}

\item{batch_col}{optional; name of batch column in colData(se)}

\item{batch_covariates}{optional character vector of covariates for batch model}

\item{filter_low_expr}{logical; drop genes expressed (>0) in < min_prop of TRAIN}

\item{min_prop}{numeric in (0,1]; minimum TRAIN proportion to keep a gene}

\item{transform}{"none" or "log1p"}

\item{standardize}{logical; z-score by TRAIN mean/SD (applied to train+test)}

\item{cv}{"kfold", "lobo", or "groupk"}

\item{auto_confounds}{logical; if TRUE, auto-switch to LOBO and enable fold batch-correction when confounded}
}
\value{
GeneRankFit S4 object
}
\description{
Performs CV on a SummarizedExperiment, aggregates fold-normalized feature
importances, and stores out-of-fold predictions. Supports K-fold, LOBO
(Leave-One-Batch-Out), and group-K by batch. Batch correction, filtering,
transforms, and standardization are applied \emph{inside folds} using TRAIN-only
statistics. If \code{auto_confounds=TRUE}, the function inspects batch~label
association and will switch to LOBO and enable fold-safe batch correction
if confounding is moderate/severe.
}
\examples{
# For reproducibility, specify a fixed seed (e.g., set.seed(1)) before running this example.

# Toy expression: 20 genes × 12 samples
expr <- matrix(stats::rnorm(20 * 12), nrow = 20)
rownames(expr) <- paste0("gene", 1:20)
colnames(expr) <- paste0("sample", 1:12)

# Binary labels
label <- factor(rep(c("A", "B"), each = 6))

se <- SummarizedExperiment::SummarizedExperiment(
  assays  = list(expr = expr),
  colData = data.frame(label = label)
)

# Rank genes with default settings
rg <- rank_genes(
  se        = se,
  label_col = "label"
)

str(rg)
}
