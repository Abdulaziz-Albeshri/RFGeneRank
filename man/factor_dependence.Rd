% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/factor_dependence.R
\name{factor_dependence}
\alias{factor_dependence}
\title{Covariate dependence of gene contributions (SHAP/proxy; "expr" assay)}
\usage{
factor_dependence(
  fit,
  se,
  covariates,
  method = c("shap", "proxy"),
  ngenes = 500L,
  gene_selection = c("importance", "variance"),
  nsim = 128L,
  bg_per_class = 64L,
  cache_dir = NULL,
  fdr_method = "BH",
  seed = 1L,
  pred_fun = NULL,
  pos_label = NULL,
  positive = NULL
)
}
\arguments{
\item{fit}{Trained object containing out-of-fold predictions (n x k) and labels.
For S4 wrappers (e.g., GeneRankFit), the finalized learner should be in
A final fitted model and the training feature names may also be present.
this function will train a temporary full-data model on-the-fly (not saved).}

\item{se}{SummarizedExperiment with assay "expr" (genes x samples).}

\item{covariates}{Character vector of covariate names in colData(se), e.g. c("sex","age").}

\item{method}{"shap" (default; uses fastshap if available) or "proxy".}

\item{ngenes}{Number of genes to test (default 500). Use "ALL" or NULL for all genes.}

\item{gene_selection}{"importance" (default) or "variance".}

\item{nsim}{SHAP Monte-Carlo permutations (default 128).}

\item{bg_per_class}{Max background samples per class for SHAP (default 64).}

\item{cache_dir}{Cache directory for SHAP matrices; default R user cache dir.}

\item{fdr_method}{Multiple-testing correction (default "BH").}

\item{seed}{RNG seed (default 1).}

\item{pred_fun}{OPTIONAL user predictor: function(newdata) -> numeric p(positive).
If provided, it takes precedence over the built-in predictor.}

\item{pos_label}{OPTIONAL (legacy) positive class label; kept for back-compat.}

\item{positive}{OPTIONAL override for positive class. Either a class label (character)
or an index 1/2. If provided, it overrides \code{pos_label}.}
}
\value{
data.frame with columns: gene, covariate, test, stat, pval, fdr, effect, dependent.
Attributes: "method","assay","genes_used","cache_file".
}
\description{
Covariate dependence of gene contributions (SHAP/proxy; "expr" assay)
}
\examples{
#' # For reproducibility, set a fixed seed such as set.seed(1) before running this example.

# Tiny expression matrix
expr <- matrix(stats::rnorm(30), nrow = 6)
rownames(expr) <- paste0("gene", 1:6)
colnames(expr) <- paste0("sample", 1:5)

# Covariates
cd <- data.frame(
  label = factor(c("A", "A", "B", "B", "B")),
  sex   = factor(c("M", "F", "F", "M", "M")),
  age   = c(30, 40, 35, 50, 60)
)

se <- SummarizedExperiment::SummarizedExperiment(
  assays  = list(expr = expr),
  colData = cd
)

# Minimal mock GeneRankFit object with required slots
prob_mat <- matrix(
  stats::runif(5),
  ncol = 1,
  dimnames = list(colnames(se), "B")
)

fit <- methods::new(
  "GeneRankFit",
  oof = list(
    prob = prob_mat,
    y    = cd$label
  )
)

}
