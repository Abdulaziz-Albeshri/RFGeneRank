% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_genes.R
\name{validate_genes}
\alias{validate_genes}
\title{Validate a ranked gene set with alternate learners via k-fold CV}
\usage{
validate_genes(
  se,
  genes,
  methods = c("glmnet", "xgboost", "ranger"),
  k = 5,
  seed = 1L,
  model_params = list(),
  label_col = "state",
  positive = NULL,
  calibrate = c("platt", "isotonic", "none"),
  thr_metric = c("youden", "f1", "cost"),
  cost = c(fp = 1, fn = 1)
)
}
\arguments{
\item{se}{A SummarizedExperiment whose assay is genes-by-samples.}

\item{genes}{Character vector of gene IDs (must match \code{rownames(assay(se))}).}

\item{methods}{Character vector; any of \code{c("ranger","glmnet","xgboost")}.}

\item{k}{Integer number of folds (default 5).}

\item{seed}{Integer RNG seed for reproducibility.}

\item{model_params}{Named list of per-method parameter lists,
e.g. \code{list(
    ranger  = list(num.trees=1000, importance="none", nthread=1),
    glmnet  = list(alpha=0.5, standardize="zscore", clip=8, eps_sd=1e-8,
                   use_class_weights=TRUE, lambda="lambda.min"),
    xgboost = list(nrounds=400, eta=0.05, max_depth=4, nthread=1)
  )}.}

\item{label_col}{Column name in \code{colData(se)} with the class labels.
Must be a binary factor (exactly \code{2} levels).}

\item{positive}{Optional; the positive class label. If \code{NULL}, uses \code{levels(y)[2]}.}

\item{calibrate}{One of \code{c("platt","isotonic","none")}; applied per fold.}

\item{thr_metric}{One of \code{c("youden","f1","cost")} for threshold selection.}

\item{cost}{Named numeric vector \code{c(fp=1, fn=1)} if \code{thr_metric == "cost"}.}
}
\value{
A list with \code{$summary} and one entry per method. Each entry contains:
\code{$oof} (data.frame with p_raw, p_cal, p_use, y, fold),
\code{$calibrator_requested}, \code{$calibrator_used}, \code{$threshold},
\code{$conf_mat}, and \code{$metrics} with both diagnostics (raw/cal)
and final guardrailed metrics (\code{auc_final}, \code{ece_final}, \code{brier_final}).
}
\description{
Validate a ranked gene set with alternate learners via k-fold CV
}
\examples{
# Toy expression matrix: 10 genes x 12 samples
expr <- matrix(
  stats::rnorm(10 * 12),
  nrow = 10,
  dimnames = list(
    paste0("gene", 1:10),
    paste0("sample", 1:12)
  )
)

# Binary labels as a factor (balanced)
label <- factor(rep(c("A", "B"), each = 6))

se <- SummarizedExperiment::SummarizedExperiment(
  assays  = list(expr = expr),
  colData = S4Vectors::DataFrame(label = label)
)

# Use the first 5 genes as a toy signature
genes <- rownames(expr)[1:5]

# Validate using ranger only (fast and robust for examples)
res <- validate_genes(
  se        = se,
  genes     = genes,
  methods   = "ranger",
  k         = 3,
  seed      = 1,
  label_col = "label"
)

# Inspect out-of-fold AUROC
res$summary$auc_final
}
