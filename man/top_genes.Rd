% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/top_genes.R
\name{top_genes}
\alias{top_genes}
\title{Extract top predictive genes from a GeneRankFit}
\usage{
top_genes(
  fit,
  n = 100,
  map = FALSE,
  OrgDb = org.Hs.eg.db::org.Hs.eg.db,
  from = "SYMBOL",
  to = "SYMBOL"
)
}
\arguments{
\item{fit}{a \code{GeneRankFit} object (from \code{rank_genes()}).}

\item{n}{integer; number of top genes to return (default 100).}

\item{map}{logical; if TRUE, map \code{from -> to} and add a \code{mapped} column.}

\item{OrgDb}{an \code{OrgDb} object for mapping (default \code{org.Hs.eg.db}).}

\item{from}{source keytype used in \code{fit@imp$gene} (e.g., "ENTREZID", "SYMBOL", "ENSEMBL").}

\item{to}{destination keytype (e.g., "SYMBOL").}
}
\value{
A list with:
\describe{
\item{gene}{character vector of top gene IDs in \code{from} keytype}
\item{table}{data.frame with columns \code{gene}, \code{importance},
\code{SelectedInFolds}, and optional \code{mapped}}
}
}
\description{
Returns a ranked list from the aggregated, fold-normalized importance
stored in \code{fit@imp}. Optionally adds an ID mapping column (e.g.,
ENTREZID -> SYMBOL) using \code{\link{id_map}}.
}
\examples{
gene_scores <- data.frame(
  gene       = paste0("gene", 1:6),
  importance = c(5, 4, 3, 2, 1, 0)
)

fit <- methods::new("GeneRankFit", imp = gene_scores)

tg <- top_genes(fit, n = 3)
tg$gene
tg$table
# In practice, top_genes() is used on a GeneRankFit object.
# For example, after running a full RFGeneRank pipeline:
#
#   fit <- gene_rank(se, genes = rownames(se), ...)
#   head(top_genes(fit, n = 20))
#
# where 'fit' is a GeneRankFit containing gene importance scores.
}
